<div class="d-sm-flex align-items-center justify-content-between">
  <h1 class="h3 mb-0 font-weight-bold">Werbliche Anzeige anlegen</h1>
</div>

<div class="row">
  <div class="col-lg-8">
    <%= form_with( url: offers_path, method: :post, local: true) do %>
      <%= render partial: "offers/form", locals: { offer: @offer } %>
    <% end %>
  </div>
  <div class="col-lg-4">
    <%= render partial: "form_hints" %>
  </div>
</div>
